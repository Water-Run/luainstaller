# Legacy Test Programs

This directory contains older demos that have not yet been promoted into the
new test taxonomy.

Keep these programs until a new sample covers the same behavior. When promoting
a legacy program, move it into one of the top-level test categories and update
that category's README.
